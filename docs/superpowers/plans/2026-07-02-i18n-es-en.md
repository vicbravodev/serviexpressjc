# Internacionalización ES/EN Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publicar la landing en español (raíz `/`) e inglés nativo (`/en`) con switcher de idioma en el header y SEO completo (hreflang, sitemap, metadata localizada, JSON-LD) para México y USA.

**Architecture:** next-intl v4 con segmento `app/[locale]/` y `localePrefix: "as-needed"` (español sin prefijo preserva URLs indexadas). Todo el copy sale de los componentes hacia `messages/es.json` y `messages/en.json`; ambas versiones se prerenderizan estáticamente (`generateStaticParams` + `setRequestLocale`). El middleware de next-intl maneja detección y cookie `NEXT_LOCALE`.

**Tech Stack:** Next.js 15.5 (App Router), React 19, next-intl v4, Tailwind v4, pnpm.

**Spec:** `docs/superpowers/specs/2026-07-02-i18n-es-en-design.md`

## Global Constraints

- Locales: `es` (default, sin prefijo de URL) y `en` (prefijo `/en`). En hreflang/OG se declaran como `es-MX` y `en-US`.
- Los IDs de anclas NO cambian entre idiomas: `#inicio`, `#quienes-somos`, `#servicios`, `#cobertura`, `#clientes`, `#talleres`, `#cotizacion`, `#postulate`.
- El copy en español actual se conserva **verbatim** (voz de marca ya validada); no reescribir ES salvo lo indicado explícitamente en este plan.
- El inglés debe leerse nativo de la industria freight de EE.UU. — usar exactamente los textos EN de este plan (ya redactados con esa intención), no re-traducir.
- No hay suite de tests en el repo: la verificación de cada task es `pnpm build` exitoso; la verificación funcional completa es la Task 9.
- Gestor de paquetes: `pnpm`. No tocar `components/ui/*` (shadcn) ni `components/motion-primitives.tsx` (no tienen copy).
- Todos los componentes de sección son sync, así que `useTranslations` funciona igual en server y client components — no usar `getTranslations` salvo en `generateMetadata`.
- Commits en español siguiendo el estilo del repo (`feat:`, `fix:`), con `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Infraestructura next-intl y reestructura `app/[locale]/`

**Files:**
- Modify: `next.config.mjs`, `package.json` (vía pnpm add)
- Create: `i18n/routing.ts`, `i18n/request.ts`, `i18n/navigation.ts`, `middleware.ts`, `global.d.ts`
- Create: `messages/es.json`, `messages/en.json` (solo namespace `Metadata` por ahora)
- Move: `app/layout.tsx` → `app/[locale]/layout.tsx`, `app/page.tsx` → `app/[locale]/page.tsx`
- Create: `app/[locale]/not-found.tsx`, `app/[locale]/[...rest]/page.tsx`
- Keep: `app/globals.css`, `app/icon.png`, `app/apple-icon.png`, `app/favicon.ico` permanecen en `app/`

**Interfaces:**
- Produces: `routing` (locales `["es","en"]`, defaultLocale `"es"`, localePrefix `"as-needed"`), `Link`/`usePathname`/`useRouter` desde `@/i18n/navigation`, mensajes tipados vía `global.d.ts`. Las tasks 2–8 dependen de esto.

- [ ] **Step 1: Instalar next-intl**

```bash
pnpm add next-intl
```

- [ ] **Step 2: Crear routing, request config y navigation**

`i18n/routing.ts`:
```ts
import { defineRouting } from "next-intl/routing"

export const routing = defineRouting({
  locales: ["es", "en"],
  defaultLocale: "es",
  localePrefix: "as-needed",
})
```

`i18n/request.ts`:
```ts
import { getRequestConfig } from "next-intl/server"
import { hasLocale } from "next-intl"
import { routing } from "./routing"

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale
  const locale = hasLocale(routing.locales, requested) ? requested : routing.defaultLocale
  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  }
})
```

`i18n/navigation.ts`:
```ts
import { createNavigation } from "next-intl/navigation"
import { routing } from "./routing"

export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing)
```

`middleware.ts` (en la raíz del repo):
```ts
import createMiddleware from "next-intl/middleware"
import { routing } from "./i18n/routing"

export default createMiddleware(routing)

export const config = {
  // Todo excepto api, internals de Next y archivos estáticos (contienen punto)
  matcher: "/((?!api|_next|_vercel|.*\\..*).*)",
}
```

`global.d.ts` (raíz; tipado estricto de claves de mensajes):
```ts
import messages from "./messages/es.json"
import { routing } from "./i18n/routing"

declare module "next-intl" {
  interface AppConfig {
    Locale: (typeof routing.locales)[number]
    Messages: typeof messages
  }
}
```

- [ ] **Step 3: Conectar el plugin en next.config.mjs**

```js
import createNextIntlPlugin from "next-intl/plugin"

const withNextIntl = createNextIntlPlugin()

/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
}

export default withNextIntl(nextConfig)
```

- [ ] **Step 4: Crear messages con namespace Metadata**

`messages/es.json`:
```json
{
  "Metadata": {
    "title": "ServiExpress JC — Transporte de Carga México–USA",
    "description": "Empresa de transporte especializada en carga nacional e internacional entre México y Estados Unidos. Más de 10 años de experiencia, monitoreo GPS 24/7."
  },
  "NotFound": {
    "title": "Página no encontrada",
    "back": "Volver al inicio"
  }
}
```

`messages/en.json`:
```json
{
  "Metadata": {
    "title": "ServiExpress JC — Cross-Border Freight Between Mexico & the U.S.",
    "description": "Trucking company specializing in domestic and cross-border freight between Mexico and the United States. 10+ years on the road, 24/7 GPS tracking, company-owned fleet."
  },
  "NotFound": {
    "title": "Page not found",
    "back": "Back to home"
  }
}
```

- [ ] **Step 5: Mover layout y page a `app/[locale]/`**

```bash
mkdir -p app/\[locale\]
git mv app/layout.tsx "app/[locale]/layout.tsx"
git mv app/page.tsx "app/[locale]/page.tsx"
```

Reescribir `app/[locale]/layout.tsx` completo:
```tsx
import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import { notFound } from "next/navigation"
import { NextIntlClientProvider, hasLocale } from "next-intl"
import { getMessages, getTranslations, setRequestLocale } from "next-intl/server"
import { routing } from "@/i18n/routing"
import "../globals.css"

const geistSans = Geist({
  subsets: ["latin"],
  variable: "--font-geist-sans",
  display: "swap",
})
const geistMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-geist-mono",
  display: "swap",
})

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }))
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>
}): Promise<Metadata> {
  const { locale } = await params
  const t = await getTranslations({ locale, namespace: "Metadata" })
  return {
    title: t("title"),
    description: t("description"),
    generator: "v0.app",
  }
}

export default async function RootLayout({
  children,
  params,
}: Readonly<{
  children: React.ReactNode
  params: Promise<{ locale: string }>
}>) {
  const { locale } = await params
  if (!hasLocale(routing.locales, locale)) notFound()
  setRequestLocale(locale)
  const messages = await getMessages()

  return (
    <html lang={locale} className={`${geistSans.variable} ${geistMono.variable}`}>
      <body className="font-sans antialiased">
        <NextIntlClientProvider messages={messages}>{children}</NextIntlClientProvider>
        <Analytics />
      </body>
    </html>
  )
}
```

En `app/[locale]/page.tsx` agregar SSG al inicio del componente (el resto queda igual):
```tsx
import { setRequestLocale } from "next-intl/server"
// ...imports existentes...

export default async function LandingPage({
  params,
}: {
  params: Promise<{ locale: string }>
}) {
  const { locale } = await params
  setRequestLocale(locale)
  // ...return existente sin cambios...
}
```

- [ ] **Step 6: 404 bilingüe**

`app/[locale]/not-found.tsx`:
```tsx
import { useTranslations } from "next-intl"
import { Link } from "@/i18n/navigation"

export default function NotFoundPage() {
  const t = useTranslations("NotFound")
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 px-4 text-center">
      <p className="font-mono text-sm uppercase tracking-[0.2em] text-muted-foreground">404</p>
      <h1 className="text-3xl font-bold">{t("title")}</h1>
      <Link href="/" className="text-primary underline underline-offset-4">
        {t("back")}
      </Link>
    </main>
  )
}
```

`app/[locale]/[...rest]/page.tsx` (atrapa rutas desconocidas, ej. `/fr`):
```tsx
import { notFound } from "next/navigation"

export default function CatchAllPage() {
  notFound()
}
```

- [ ] **Step 7: Verificar build**

Run: `pnpm build`
Expected: build exitoso; en el output aparecen `/[locale]` prerenderizado con `/es` y `/en` (SSG). La página sigue mostrándose 100% en español en ambas rutas (el copy aún está hardcodeado).

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat(i18n): infraestructura next-intl con rutas /(es) y /en prerenderizadas"
```

---

### Task 2: Archivos de mensajes completos (ES verbatim + EN nativo)

**Files:**
- Modify: `messages/es.json`, `messages/en.json`

**Interfaces:**
- Produces: todos los namespaces/claves que consumen las Tasks 3–8: `Header`, `Hero`, `Stats`, `Clients`, `Features`, `Services`, `Certifications`, `Talleres`, `About`, `Coverage`, `Testimonials`, `CTA`, `Footer`, `LanguageSwitcher` (además de `Metadata` y `NotFound` ya creados). Acceso a arrays por índice: `t("items.0.title")`.

- [ ] **Step 1: Escribir `messages/es.json` completo**

Reemplazar el archivo entero con (ES extraído verbatim de los componentes):
```json
{
  "Metadata": {
    "title": "ServiExpress JC — Transporte de Carga México–USA",
    "description": "Empresa de transporte especializada en carga nacional e internacional entre México y Estados Unidos. Más de 10 años de experiencia, monitoreo GPS 24/7."
  },
  "NotFound": {
    "title": "Página no encontrada",
    "back": "Volver al inicio"
  },
  "LanguageSwitcher": {
    "label": "Idioma",
    "switchToEn": "Switch to English",
    "switchToEs": "Cambiar a español"
  },
  "Header": {
    "nav": {
      "inicio": "Inicio",
      "quienesSomos": "Quiénes Somos",
      "servicios": "Servicios",
      "cobertura": "Cobertura",
      "clientes": "Clientes"
    },
    "apply": "Postúlate",
    "quote": "Cotización",
    "openMenu": "Abrir menú",
    "closeMenu": "Cerrar menú"
  },
  "Hero": {
    "badge": "Operación en vivo",
    "badgeYears": " · 10+ años en ruta",
    "titleLead": "Eficiencia sobre ruedas,",
    "titleAccent": "compromiso sin fronteras",
    "subtitle": "Transporte especializado de carga entre México y Estados Unidos. Cada unidad, monitoreada por GPS las 24 horas, desde nuestra sala de control hasta tu cliente final.",
    "cta": "Solicita tu cotización",
    "watch": "Ver presentación",
    "watchMeta": "2 min · con audio",
    "soundOn": "Sonido activo",
    "soundOff": "Activar sonido",
    "muteAria": "Silenciar video",
    "unmuteAria": "Activar sonido del video",
    "fullscreenAria": "Ver presentación en pantalla completa",
    "modalAria": "Presentación de ServiExpress JC",
    "closeAria": "Cerrar presentación"
  },
  "Stats": {
    "console": "Monitoreo en tiempo real · 24/7",
    "consoleTag": "SERVIEXPRESS·JC / OPS",
    "metrics": [
      { "label": "Años en ruta", "caption": "Operación continua MX–USA" },
      { "label": "Monitoreo GPS", "caption": "Rastreo en tiempo real" },
      { "label": "Flota propia", "caption": "Sin subcontratar tu carga" },
      { "label": "Operadores", "caption": "Certificados para cruce" }
    ]
  },
  "Clients": {
    "title": "Empresas que confían en nosotros",
    "subtitle": "Trabajamos con las empresas más importantes de México y Estados Unidos",
    "logoAlt": "Logotipo de {name}",
    "more": "Y muchas más empresas que confían en nuestro servicio de transporte"
  },
  "Features": {
    "kicker": "Por qué ServiExpress JC",
    "title": "Razones para confiarnos tu carga",
    "subtitle": "Ventajas concretas, sostenidas con evidencia, no con adjetivos.",
    "items": [
      { "title": "Flota moderna y propia", "description": "Unidades con doble placa y remolques de 53 pies con suspensión de aire." },
      { "title": "Seguridad 24/7", "description": "Monitoreo GPS en tiempo real para entregas puntuales y seguras." },
      { "title": "Cobertura amplia", "description": "Servicio nacional e internacional México–USA con múltiples rutas." },
      { "title": "Entregas puntuales", "description": "Compromiso con los tiempos de entrega y atención personalizada." },
      { "title": "5 talleres propios", "description": "Mantenimiento en casa y refaccionaria Los César con inventario extenso." },
      { "title": "Operadores certificados", "description": "Personal altamente capacitado con certificación B1 para el cruce." }
    ]
  },
  "Services": {
    "kicker": "Servicios",
    "title": "Lo que movemos, probado en carretera",
    "subtitle": "Carga nacional e internacional, con unidades propias y operadores certificados de punta a punta.",
    "national": {
      "badge": "Nacional",
      "imageAlt": "Unidad de ServiExpress JC transportando carga siderúrgica de acero",
      "imageCaption": "Carga de acero · nacional",
      "title": "Transporte nacional",
      "description": "Local, regional y larga distancia por todo México. Carga siderúrgica, agrícola, seca y sobredimensionada, monitoreada por GPS de origen a destino.",
      "zones": ["Norte", "Bajío", "Pacífico", "Occidente"],
      "cta": "Ver cobertura"
    },
    "international": {
      "badge": "Internacional · MX → USA",
      "imageAlt": "Tractor de ServiExpress JC en báscula certificada en Estados Unidos",
      "imageCaption": "Cruce México · USA",
      "title": "Transporte internacional",
      "description": "Servicio transfronterizo puerta a puerta. Cruce por Laredo, TX con operadores certificados B1 y unidades de doble placa.",
      "zones": ["Texas", "Midwest", "Southeast", "West"],
      "cta": "Solicitar cotización"
    },
    "specialized": {
      "kicker": "Carga especializada",
      "imageAlt": "Unidad de ServiExpress JC cargada con pacas de forraje agrícola",
      "imageCaption": "Carga agrícola · forraje",
      "title": "Una flota, muchos tipos de carga",
      "description": "De varilla y perfiles de acero a pacas de forraje: plataformas, cajas secas de 53 pies y equipo para carga sobredimensionada, siempre amarrada y monitoreada.",
      "tags": ["Siderúrgica / acero", "Agrícola y forraje", "Carga seca (53')", "Sobredimensionada"]
    }
  },
  "Certifications": {
    "title": "Certificaciones y Garantías",
    "subtitle": "Cumplimos con todos los estándares de calidad y seguridad del sector",
    "items": [
      { "title": "Certificación B1", "description": "Operadores certificados con licencia federal tipo B1 para transporte de carga" },
      { "title": "Doble Placa", "description": "Vehículos con doble placa México-USA para cruce fronterizo sin complicaciones" },
      { "title": "Seguros Completos", "description": "Cobertura total de seguro de carga para proteger tu mercancía" },
      { "title": "Flota Certificada", "description": "Unidades con verificación vigente y mantenimiento preventivo constante" }
    ]
  },
  "Talleres": {
    "kicker": "Mantenimiento propio",
    "titleLead": "5 talleres propios.",
    "titleAccent": "Cero pretextos.",
    "description": "No subcontratamos ni el mantenimiento. Cinco talleres con gente completamente capacitada atienden nuestras unidades, así tu carga viaja en equipo confiable y tus entregas no dependen de un tercero.",
    "imageAlt": "Kenworth clásico de ServiExpress JC restaurado y mantenido en sus talleres propios",
    "imageCaption": "Unidad mantenida en talleres propios",
    "overlay": "Cuidamos cada unidad como esta",
    "pillars": [
      { "title": "5 talleres propios", "caption": "Mantenimiento preventivo y correctivo en casa" },
      { "title": "Personal capacitado", "caption": "Técnicos especializados en cada unidad" },
      { "title": "Sin depender de terceros", "caption": "Las entregas no se detienen por una falla" },
      { "title": "Refaccionaria propia", "caption": "Los César, con inventario extenso" }
    ]
  },
  "About": {
    "kicker": "Quiénes somos",
    "title": "Gente real detrás de cada entrega",
    "subtitle": "ServiExpress JC es una empresa de transporte con más de una década de experiencia en carga nacional e internacional. Flota propia, operada y mantenida por nuestra propia gente.",
    "fleetAlt": "Flota de tractocamiones de ServiExpress JC en su terminal",
    "fleetCaption": "Flota propia · terminal en Apodaca, N.L.",
    "fleetChip": "Flota propia",
    "fleetLocation": "Terminal · Apodaca, N.L.",
    "missionTitle": "Nuestra Misión",
    "missionText": "Garantizar un servicio de calidad que cubra las necesidades de nuestros clientes, coordinando y monitoreando el traslado con el mejor personal.",
    "visionTitle": "Nuestra Visión",
    "visionText": "Ser reconocidos como una de las empresas de transporte más competitivas en México y EE.UU., incrementando nuestro valor año con año.",
    "valuesTitle": "Nuestros Valores",
    "values": [
      "Calidad en el servicio",
      "Innovación tecnológica",
      "Seguridad 24/7",
      "Entregas puntuales",
      "Atención personalizada",
      "Compromiso con el cliente"
    ]
  },
  "Coverage": {
    "kicker": "Red de operación",
    "title": "Cobertura nacional e internacional",
    "subtitle": "Un corredor monitoreado punto a punto, desde el Bajío hasta el sureste de Estados Unidos.",
    "console": "Corredor principal",
    "consoleTag": "MTY → LAREDO → HOU",
    "mapAria": "Mapa del corredor de transporte México - Estados Unidos",
    "borderLabel": "FRONTERA MX · USA",
    "telemetry": "1,180 km · ETA 17 h",
    "crossingChip": "Cruce B1 · doble placa",
    "mxTitle": "México",
    "usTitle": "Estados Unidos",
    "citiesCount": "{count} ciudades",
    "mxZones": { "norte": "Norte", "bajio": "Bajío", "pacifico": "Pacífico", "occidente": "Occidente" },
    "usZones": { "texas": "Texas", "midwest": "Midwest", "southeast": "Southeast", "west": "West" }
  },
  "Testimonials": {
    "kicker": "Clientes",
    "title": "Lo que dicen quienes ya confían",
    "subtitle": "Acereras y exportadores que mueven su carga con nosotros.",
    "items": [
      {
        "quote": "Confiamos en SERVIEXPRESS JC para nuestras rutas del norte y ahora también para exportación. Siempre puntuales y con excelente seguimiento.",
        "role": "Logística y Abastecimiento"
      },
      {
        "quote": "El servicio de monitoreo GPS nos da tranquilidad total. Sabemos exactamente dónde está nuestra carga en todo momento.",
        "role": "Cadena de Suministro"
      },
      {
        "quote": "Profesionalismo y atención personalizada. Han sido clave para el crecimiento de nuestro negocio.",
        "role": "Operaciones y Distribución"
      }
    ]
  },
  "CTA": {
    "quote": {
      "kicker": "Clientes · B2B",
      "title": "Solicita tu cotización",
      "description": "Cuéntanos tu ruta y tu carga; te respondemos con una propuesta a la medida.",
      "name": "Nombre completo",
      "namePh": "Ej. María Hernández",
      "email": "Correo electrónico",
      "emailPh": "nombre@empresa.com",
      "phone": "Teléfono",
      "phonePh": "Ej. 81 1234 5678",
      "company": "Empresa",
      "companyPh": "Nombre de tu empresa",
      "origin": "Origen",
      "originPh": "Ciudad de origen",
      "destination": "Destino",
      "destinationPh": "Ciudad de destino",
      "details": "Detalles de la carga",
      "detailsPh": "Tipo, peso y dimensiones de la carga",
      "submit": "Enviar solicitud"
    },
    "apply": {
      "kicker": "Operadores · bolsa de trabajo",
      "title": "Únete a nuestro equipo",
      "description": "Buscamos operadores certificados y personal comprometido.",
      "name": "Nombre completo",
      "namePh": "Ej. Juan Pérez",
      "email": "Correo electrónico",
      "emailPh": "nombre@correo.com",
      "phone": "Teléfono",
      "phonePh": "Ej. 81 1234 5678",
      "position": "Puesto de interés",
      "positionPh": "Ej. Operador B1",
      "experience": "Tu experiencia",
      "experiencePh": "Cuéntanos sobre tu experiencia",
      "cv": "Adjuntar CV (opcional)",
      "submit": "Enviar postulación"
    },
    "whatsapp": {
      "title": "¿Necesitas ayuda inmediata?",
      "description": "Escríbenos por WhatsApp y te respondemos en minutos.",
      "button": "Chatear en WhatsApp"
    }
  },
  "Footer": {
    "tagline": "Eficiencia sobre ruedas, compromiso sin fronteras",
    "quickLinksTitle": "Enlaces Rápidos",
    "servicesTitle": "Servicios",
    "services": [
      "Transporte Nacional",
      "Transporte Internacional",
      "Monitoreo GPS 24/7",
      "Servicio Puerta a Puerta",
      "Refaccionaria Los César"
    ],
    "contactTitle": "Contacto",
    "addressLine1": "Carretera Mezquital Santa Rosa Km 05",
    "addressLine2": "Apodaca, N.L., México",
    "rights": "Todos los derechos reservados."
  }
}
```

- [ ] **Step 2: Escribir `messages/en.json` completo (inglés nativo freight US)**

Reemplazar el archivo entero con:
```json
{
  "Metadata": {
    "title": "ServiExpress JC — Cross-Border Freight Between Mexico & the U.S.",
    "description": "Trucking company specializing in domestic and cross-border freight between Mexico and the United States. 10+ years on the road, 24/7 GPS tracking, company-owned fleet."
  },
  "NotFound": {
    "title": "Page not found",
    "back": "Back to home"
  },
  "LanguageSwitcher": {
    "label": "Language",
    "switchToEn": "Switch to English",
    "switchToEs": "Cambiar a español"
  },
  "Header": {
    "nav": {
      "inicio": "Home",
      "quienesSomos": "About Us",
      "servicios": "Services",
      "cobertura": "Coverage",
      "clientes": "Clients"
    },
    "apply": "Apply Now",
    "quote": "Get a Quote",
    "openMenu": "Open menu",
    "closeMenu": "Close menu"
  },
  "Hero": {
    "badge": "Live operations",
    "badgeYears": " · 10+ years on the road",
    "titleLead": "Efficiency on wheels,",
    "titleAccent": "commitment beyond borders",
    "subtitle": "Specialized freight transport between Mexico and the United States. Every truck tracked by GPS around the clock — from our control room to your end customer.",
    "cta": "Request your quote",
    "watch": "Watch the video",
    "watchMeta": "2 min · Spanish audio",
    "soundOn": "Sound on",
    "soundOff": "Unmute",
    "muteAria": "Mute video",
    "unmuteAria": "Unmute video",
    "fullscreenAria": "Watch the presentation in fullscreen",
    "modalAria": "ServiExpress JC company presentation",
    "closeAria": "Close presentation"
  },
  "Stats": {
    "console": "Real-time monitoring · 24/7",
    "consoleTag": "SERVIEXPRESS·JC / OPS",
    "metrics": [
      { "label": "Years on the road", "caption": "Continuous MX–USA operations" },
      { "label": "GPS tracking", "caption": "Real-time visibility" },
      { "label": "Company-owned fleet", "caption": "We never broker your freight" },
      { "label": "Drivers", "caption": "Certified for the border crossing" }
    ]
  },
  "Clients": {
    "title": "Companies that trust us",
    "subtitle": "We work with leading companies across Mexico and the United States",
    "logoAlt": "{name} logo",
    "more": "And many more companies that rely on our freight services"
  },
  "Features": {
    "kicker": "Why ServiExpress JC",
    "title": "Reasons to trust us with your freight",
    "subtitle": "Concrete advantages, backed by evidence — not adjectives.",
    "items": [
      { "title": "Modern, company-owned fleet", "description": "Dual-plated trucks and 53-foot air-ride trailers." },
      { "title": "24/7 security", "description": "Real-time GPS tracking for safe, on-time deliveries." },
      { "title": "Broad coverage", "description": "Domestic and cross-border Mexico–U.S. service across multiple lanes." },
      { "title": "On-time delivery", "description": "Committed to delivery windows, with personalized attention." },
      { "title": "5 in-house repair shops", "description": "In-house maintenance plus our Los César parts store with deep inventory." },
      { "title": "Certified drivers", "description": "Highly trained drivers holding B1 certification for the border crossing." }
    ]
  },
  "Services": {
    "kicker": "Services",
    "title": "What we haul, proven on the road",
    "subtitle": "Domestic and cross-border freight, with our own trucks and certified drivers end to end.",
    "national": {
      "badge": "Domestic",
      "imageAlt": "ServiExpress JC truck hauling steel freight",
      "imageCaption": "Steel freight · domestic",
      "title": "Domestic freight",
      "description": "Local, regional, and long-haul service across Mexico. Steel, agricultural, dry, and oversized freight — GPS-tracked from origin to destination.",
      "zones": ["North", "Bajío", "Pacific", "West"],
      "cta": "View coverage"
    },
    "international": {
      "badge": "Cross-border · MX → USA",
      "imageAlt": "ServiExpress JC tractor at a certified scale in the United States",
      "imageCaption": "Mexico–U.S. crossing",
      "title": "Cross-border freight",
      "description": "Door-to-door cross-border service. Crossing at Laredo, TX with B1-certified drivers and dual-plated trucks.",
      "zones": ["Texas", "Midwest", "Southeast", "West"],
      "cta": "Get a quote"
    },
    "specialized": {
      "kicker": "Specialized freight",
      "imageAlt": "ServiExpress JC truck loaded with agricultural hay bales",
      "imageCaption": "Agricultural freight · forage",
      "title": "One fleet, many kinds of freight",
      "description": "From rebar and steel profiles to hay bales: flatbeds, 53-foot dry vans, and equipment for oversized loads — always secured and tracked.",
      "tags": ["Steel", "Agricultural & forage", "Dry freight (53')", "Oversized"]
    }
  },
  "Certifications": {
    "title": "Certifications & Guarantees",
    "subtitle": "We meet every quality and safety standard in the industry",
    "items": [
      { "title": "B1 Certification", "description": "Drivers certified with a federal B1 license for freight transport" },
      { "title": "Dual Plates", "description": "Trucks with dual Mexico–U.S. plates for a hassle-free border crossing" },
      { "title": "Full Cargo Insurance", "description": "Comprehensive cargo insurance coverage to protect your goods" },
      { "title": "Certified Fleet", "description": "Trucks with current inspections and ongoing preventive maintenance" }
    ]
  },
  "Talleres": {
    "kicker": "In-house maintenance",
    "titleLead": "5 in-house shops.",
    "titleAccent": "Zero excuses.",
    "description": "We don't outsource anything — not even maintenance. Five shops staffed by fully trained technicians service our fleet, so your freight travels on reliable equipment and your deliveries never depend on a third party.",
    "imageAlt": "Classic ServiExpress JC Kenworth, restored and maintained in our own shops",
    "imageCaption": "Maintained in our own shops",
    "overlay": "Every truck gets this level of care",
    "pillars": [
      { "title": "5 in-house shops", "caption": "Preventive and corrective maintenance, in-house" },
      { "title": "Trained technicians", "caption": "Specialists on every truck" },
      { "title": "No third-party dependence", "caption": "A breakdown never stops your delivery" },
      { "title": "Our own parts store", "caption": "Los César, with deep inventory" }
    ]
  },
  "About": {
    "kicker": "About us",
    "title": "Real people behind every delivery",
    "subtitle": "ServiExpress JC is a trucking company with more than a decade of experience in domestic and cross-border freight. A company-owned fleet, driven and maintained by our own people.",
    "fleetAlt": "ServiExpress JC tractor fleet at our terminal",
    "fleetCaption": "Company-owned fleet · Apodaca, N.L. terminal",
    "fleetChip": "Company-owned fleet",
    "fleetLocation": "Terminal · Apodaca, N.L.",
    "missionTitle": "Our Mission",
    "missionText": "To deliver a quality service that meets our customers' needs, coordinating and monitoring every shipment with the best people.",
    "visionTitle": "Our Vision",
    "visionText": "To be recognized as one of the most competitive trucking companies in Mexico and the U.S., growing our value year after year.",
    "valuesTitle": "Our Values",
    "values": [
      "Service quality",
      "Technological innovation",
      "24/7 security",
      "On-time delivery",
      "Personalized attention",
      "Commitment to our customers"
    ]
  },
  "Coverage": {
    "kicker": "Operating network",
    "title": "Domestic and cross-border coverage",
    "subtitle": "A corridor monitored point to point, from the Bajío to the southeastern United States.",
    "console": "Main corridor",
    "consoleTag": "MTY → LAREDO → HOU",
    "mapAria": "Map of the Mexico–United States freight corridor",
    "borderLabel": "MX · USA BORDER",
    "telemetry": "733 mi · ETA 17 h",
    "crossingChip": "B1 crossing · dual plates",
    "mxTitle": "Mexico",
    "usTitle": "United States",
    "citiesCount": "{count} cities",
    "mxZones": { "norte": "North", "bajio": "Bajío", "pacifico": "Pacific", "occidente": "West" },
    "usZones": { "texas": "Texas", "midwest": "Midwest", "southeast": "Southeast", "west": "West" }
  },
  "Testimonials": {
    "kicker": "Clients",
    "title": "What our customers say",
    "subtitle": "Steel producers and exporters who move their freight with us.",
    "items": [
      {
        "quote": "We trust SERVIEXPRESS JC with our northern routes, and now with our exports as well. Always on time, with excellent tracking.",
        "role": "Logistics & Procurement"
      },
      {
        "quote": "Their GPS monitoring gives us total peace of mind. We know exactly where our freight is at all times.",
        "role": "Supply Chain"
      },
      {
        "quote": "Professionalism and personalized attention. They have been key to the growth of our business.",
        "role": "Operations & Distribution"
      }
    ]
  },
  "CTA": {
    "quote": {
      "kicker": "Customers · B2B",
      "title": "Request your quote",
      "description": "Tell us your lane and your freight — we'll come back with a tailored proposal.",
      "name": "Full name",
      "namePh": "e.g. Sarah Johnson",
      "email": "Email",
      "emailPh": "name@company.com",
      "phone": "Phone",
      "phonePh": "e.g. (346) 366-9867",
      "company": "Company",
      "companyPh": "Your company name",
      "origin": "Origin",
      "originPh": "Origin city",
      "destination": "Destination",
      "destinationPh": "Destination city",
      "details": "Freight details",
      "detailsPh": "Type, weight, and dimensions of the load",
      "submit": "Send request"
    },
    "apply": {
      "kicker": "Drivers · careers",
      "title": "Join our team",
      "description": "We're hiring certified drivers and committed team members.",
      "name": "Full name",
      "namePh": "e.g. John Smith",
      "email": "Email",
      "emailPh": "name@email.com",
      "phone": "Phone",
      "phonePh": "e.g. (346) 366-9867",
      "position": "Position of interest",
      "positionPh": "e.g. B1 driver",
      "experience": "Your experience",
      "experiencePh": "Tell us about your experience",
      "cv": "Attach résumé (optional)",
      "submit": "Submit application"
    },
    "whatsapp": {
      "title": "Need help right away?",
      "description": "Message us on WhatsApp and we'll get back to you in minutes.",
      "button": "Chat on WhatsApp"
    }
  },
  "Footer": {
    "tagline": "Efficiency on wheels, commitment beyond borders",
    "quickLinksTitle": "Quick Links",
    "servicesTitle": "Services",
    "services": [
      "Domestic Freight",
      "Cross-Border Freight",
      "24/7 GPS Tracking",
      "Door-to-Door Service",
      "Los César Parts Store"
    ],
    "contactTitle": "Contact",
    "addressLine1": "Carretera Mezquital Santa Rosa Km 05",
    "addressLine2": "Apodaca, N.L., Mexico",
    "rights": "All rights reserved."
  }
}
```

- [ ] **Step 3: Verificar paridad de claves entre ambos JSON**

Run:
```bash
node -e "
const es = require('./messages/es.json'), en = require('./messages/en.json');
const keys = (o, p='') => Object.entries(o).flatMap(([k,v]) => typeof v === 'object' && v !== null ? keys(v, p+k+'.') : [p+k]);
const a = new Set(keys(es)), b = new Set(keys(en));
const onlyEs = [...a].filter(k => !b.has(k)), onlyEn = [...b].filter(k => !a.has(k));
if (onlyEs.length || onlyEn.length) { console.error({onlyEs, onlyEn}); process.exit(1); }
console.log('OK: ' + a.size + ' claves en paridad');
"
```
Expected: `OK: <n> claves en paridad`

- [ ] **Step 4: Commit**

```bash
git add messages/
git commit -m "feat(i18n): mensajes completos ES (verbatim) y EN nativo freight US"
```

---

### Task 3: Header traducido + switcher de idioma

**Files:**
- Create: `components/language-switcher.tsx`
- Modify: `components/header.tsx`

**Interfaces:**
- Consumes: namespaces `Header` y `LanguageSwitcher` (Task 2); `usePathname`/`useRouter` de `@/i18n/navigation` (Task 1).
- Produces: `<LanguageSwitcher solid={boolean} />` — prop `solid` controla el esquema de color sobre hero transparente vs header sólido.

- [ ] **Step 1: Crear el switcher**

`components/language-switcher.tsx`:
```tsx
"use client"

import { useLocale, useTranslations } from "next-intl"
import { usePathname, useRouter } from "@/i18n/navigation"

export function LanguageSwitcher({ solid }: { solid: boolean }) {
  const locale = useLocale()
  const router = useRouter()
  const pathname = usePathname()
  const t = useTranslations("LanguageSwitcher")

  const switchTo = (next: "es" | "en") => {
    if (next === locale) return
    router.replace(pathname, { locale: next, scroll: false })
  }

  const base = "min-h-11 px-2 font-mono text-sm font-medium uppercase tracking-wider transition-colors"
  const active = "text-yellow-accent"
  const idle = solid ? "text-foreground/60 hover:text-foreground" : "text-white/60 hover:text-white"

  return (
    <div className="flex items-center" role="group" aria-label={t("label")}>
      <button
        type="button"
        onClick={() => switchTo("es")}
        aria-pressed={locale === "es"}
        aria-label={t("switchToEs")}
        className={`${base} ${locale === "es" ? active : idle}`}
      >
        ES
      </button>
      <span className={solid ? "text-foreground/30" : "text-white/30"}>|</span>
      <button
        type="button"
        onClick={() => switchTo("en")}
        aria-pressed={locale === "en"}
        aria-label={t("switchToEn")}
        className={`${base} ${locale === "en" ? active : idle}`}
      >
        EN
      </button>
    </div>
  )
}
```

- [ ] **Step 2: Traducir el header e integrar el switcher**

En `components/header.tsx`:

1. Agregar imports y hook (el `Link` de next sigue siendo válido para anclas `#...`, pero el logo debe conservar el locale):
```tsx
import { useTranslations } from "next-intl"
import { Link as LocaleLink } from "@/i18n/navigation"
import { LanguageSwitcher } from "@/components/language-switcher"
```
y dentro del componente: `const t = useTranslations("Header")`.

2. Reemplazar el array `navItems`:
```tsx
const navItems = [
  { label: t("nav.inicio"), href: "#inicio" },
  { label: t("nav.quienesSomos"), href: "#quienes-somos" },
  { label: t("nav.servicios"), href: "#servicios" },
  { label: t("nav.cobertura"), href: "#cobertura" },
  { label: t("nav.clientes"), href: "#clientes" },
]
```

3. El logo: `<Link href="/" ...>` → `<LocaleLink href="/" ...>` (cierre incluido).

4. Strings de CTAs: `Postúlate` → `{t("apply")}` (2 lugares: desktop y móvil); `Cotización` → `{t("quote")}` (2 lugares).

5. Aria del botón móvil: `aria-label={isMobileMenuOpen ? "Cerrar menú" : "Abrir menú"}` → `aria-label={isMobileMenuOpen ? t("closeMenu") : t("openMenu")}`.

6. Insertar el switcher en desktop, dentro del div de CTA buttons, antes del primer `<Button>`:
```tsx
<div className="hidden md:flex items-center gap-4">
  <LanguageSwitcher solid={solid} />
  {/* ...botones existentes... */}
```
y en el menú móvil, dentro del div `flex flex-col gap-3 pt-4 border-t border-border`, como primer hijo:
```tsx
<LanguageSwitcher solid />
```

- [ ] **Step 3: Verificar build**

Run: `pnpm build`
Expected: build exitoso.

- [ ] **Step 4: Commit**

```bash
git add components/header.tsx components/language-switcher.tsx
git commit -m "feat(i18n): header traducido y switcher ES|EN en desktop y móvil"
```

---

### Task 4: Hero y Stats traducidos

**Files:**
- Modify: `components/hero-section.tsx`, `components/stats-section.tsx`

**Interfaces:**
- Consumes: namespaces `Hero` y `Stats` (Task 2). Ambos son client components; reciben mensajes vía `NextIntlClientProvider` (Task 1).

- [ ] **Step 1: Traducir hero-section**

En `components/hero-section.tsx` agregar `import { useTranslations } from "next-intl"` y `const t = useTranslations("Hero")` dentro del componente. Reemplazos:

| Actual | Nuevo |
|---|---|
| `Operación en vivo<span className="hidden sm:inline"> · 10+ años en ruta</span>` | `{t("badge")}<span className="hidden sm:inline">{t("badgeYears")}</span>` |
| `Eficiencia sobre ruedas,{" "}<span className="text-yellow-accent-bright">compromiso sin fronteras</span>` | `{t("titleLead")}{" "}<span className="text-yellow-accent-bright">{t("titleAccent")}</span>` |
| párrafo `Transporte especializado de carga...cliente final.` | `{t("subtitle")}` |
| `Solicita tu cotización` | `{t("cta")}` |
| `Ver presentación` | `{t("watch")}` |
| `2 min · con audio` | `{t("watchMeta")}` |
| `aria-label={soundOn ? "Silenciar video" : "Activar sonido del video"}` | `aria-label={soundOn ? t("muteAria") : t("unmuteAria")}` |
| `{soundOn ? "Sonido activo" : "Activar sonido"}` | `{soundOn ? t("soundOn") : t("soundOff")}` |
| `aria-label="Ver presentación en pantalla completa"` | `aria-label={t("fullscreenAria")}` |
| `aria-label="Presentación de ServiExpress JC"` | `aria-label={t("modalAria")}` |
| `aria-label="Cerrar presentación"` | `aria-label={t("closeAria")}` |

- [ ] **Step 2: Traducir stats-section**

En `components/stats-section.tsx` agregar `import { useTranslations } from "next-intl"`. El array `metrics` pierde `label`/`caption` (se quedan íconos y valores):
```tsx
const metrics = [
  { icon: BadgeCheck, target: 10, suffix: "+" },
  { icon: Clock, target: null, display: "24/7" },
  { icon: Truck, target: 100, suffix: "%" },
  { icon: ShieldCheck, target: null, display: "B1" },
] as const
```
Actualizar el tipo `Metric` quitando `label` y `caption` (o eliminarlo y tipar inline). `MetricCell` recibe el texto por índice — dentro de `MetricCell` agregar `const t = useTranslations("Stats")` y reemplazar:
- `{metric.label}` → `{t(`metrics.${index}.label`)}`
- `{metric.caption}` → `{t(`metrics.${index}.caption`)}`

En `StatsSection`: `const t = useTranslations("Stats")`, reemplazar `Monitoreo en tiempo real · 24/7` → `{t("console")}` y `SERVIEXPRESS·JC / OPS` → `{t("consoleTag")}`. El `key={metric.label}` del map cambia a `key={i}`.

- [ ] **Step 3: Verificar build**

Run: `pnpm build`
Expected: build exitoso.

- [ ] **Step 4: Commit**

```bash
git add components/hero-section.tsx components/stats-section.tsx
git commit -m "feat(i18n): hero y stats consumen mensajes ES/EN"
```

---

### Task 5: Clients, Features, Services y Certifications traducidos

**Files:**
- Modify: `components/clients-section.tsx`, `components/features-section.tsx`, `components/services-section.tsx`, `components/certifications-section.tsx`

**Interfaces:**
- Consumes: namespaces `Clients`, `Features`, `Services`, `Certifications` (Task 2). Son server components sync — `useTranslations` funciona directo.

- [ ] **Step 1: clients-section**

`import { useTranslations } from "next-intl"`, `const t = useTranslations("Clients")`. Reemplazos: título → `{t("title")}`, subtítulo → `{t("subtitle")}`, `alt={`Logotipo de ${client.name}`}` → `alt={t("logoAlt", { name: client.name })}`, texto final → `{t("more")}`. El array `clients` (nombres/logos) no cambia.

- [ ] **Step 2: features-section**

`const t = useTranslations("Features")`. El array `features` conserva solo íconos:
```tsx
const featureIcons = [Truck, Shield, MapPin, Clock, Wrench, Users]
```
El map se convierte en:
```tsx
{featureIcons.map((Icon, index) => (
  <RevealChild key={index} className="group flex items-start gap-5 border-b border-border py-8">
    <span className="mt-1 font-mono text-xs tabular-nums text-muted-foreground transition-colors group-hover:text-yellow-accent">
      {String(index + 1).padStart(2, "0")}
    </span>
    <Icon
      className="mt-0.5 h-6 w-6 shrink-0 text-primary transition-transform duration-300 group-hover:scale-110"
      strokeWidth={1.5}
    />
    <div>
      <h3 className="mb-2 text-xl font-semibold">{t(`items.${index}.title`)}</h3>
      <p className="leading-relaxed text-muted-foreground">{t(`items.${index}.description`)}</p>
    </div>
  </RevealChild>
))}
```
Kicker/título/subtítulo → `{t("kicker")}`, `{t("title")}`, `{t("subtitle")}`.

- [ ] **Step 3: services-section**

`const t = useTranslations("Services")`. Reemplazar los arrays de zonas:
```tsx
const zoneIndexes = [0, 1, 2, 3]
```
y en cada card mapear `zoneIndexes.map((i) => ... {t(`national.zones.${i}`)} ...)` / `{t(`international.zones.${i}`)}`. Los tags de carga especializada:
```tsx
{[0, 1, 2, 3].map((i) => (
  <div key={i} className="flex items-center gap-2 text-sm">
    <span className="h-1.5 w-1.5 shrink-0 rounded-full bg-yellow-accent" />
    <span className="text-foreground">{t(`specialized.tags.${i}`)}</span>
  </div>
))}
```
Reemplazos de strings restantes (todos con su clave homónima del namespace `Services`): kicker, title, subtitle; en la card nacional: badge `Nacional` → `{t("national.badge")}`, `alt` → `{t("national.imageAlt")}`, `caption` → `{t("national.imageCaption")}`, h3 → `{t("national.title")}`, párrafo → `{t("national.description")}`, botón `Ver cobertura` → `{t("national.cta")}`; en la internacional lo mismo con `international.*` (badge `Internacional · MX → USA`, alt, caption, h3, párrafo, botón `Solicitar cotización` → `{t("international.cta")}`); en el bloque de carga especializada: kicker → `{t("specialized.kicker")}`, alt/caption → `specialized.imageAlt`/`specialized.imageCaption`, h3 → `{t("specialized.title")}`, párrafo → `{t("specialized.description")}`.

- [ ] **Step 4: certifications-section**

`const t = useTranslations("Certifications")`. Array reduce a íconos:
```tsx
const certIcons = [Shield, Award, FileCheck, Truck]
```
Map:
```tsx
{certIcons.map((Icon, index) => (
  <div key={index} className="group bg-card p-8 flex flex-col items-start gap-4 transition-colors duration-300 hover:bg-muted/40">
    <div className="flex items-center justify-center w-12 h-12 rounded-lg bg-primary/10 text-primary transition-colors duration-300 group-hover:bg-primary group-hover:text-primary-foreground">
      <Icon className="w-6 h-6" strokeWidth={1.5} />
    </div>
    <h3 className="text-lg font-semibold leading-snug">{t(`items.${index}.title`)}</h3>
    <p className="text-muted-foreground leading-relaxed text-sm">{t(`items.${index}.description`)}</p>
  </div>
))}
```
Título/subtítulo → `{t("title")}` / `{t("subtitle")}`.

- [ ] **Step 5: Verificar build y commit**

Run: `pnpm build` — Expected: exitoso.
```bash
git add components/clients-section.tsx components/features-section.tsx components/services-section.tsx components/certifications-section.tsx
git commit -m "feat(i18n): clients, features, services y certifications consumen mensajes"
```

---

### Task 6: Talleres, About, Coverage y Testimonials traducidos

**Files:**
- Modify: `components/talleres-section.tsx`, `components/about-section.tsx`, `components/coverage-section.tsx`, `components/testimonials-section.tsx`

**Interfaces:**
- Consumes: namespaces `Talleres`, `About`, `Coverage`, `Testimonials` (Task 2). `coverage-section` es client component (mensajes vía provider).

- [ ] **Step 1: talleres-section**

`const t = useTranslations("Talleres")`. Array `pillars` reduce a íconos: `const pillarIcons = [Wrench, Users, ShieldCheck, Package]`; el map usa `pillarIcons.map((Icon, i) => ...)` con `key={i}`, `{t(`pillars.${i}.title`)}` y `{t(`pillars.${i}.caption`)}`. Strings: kicker → `{t("kicker")}`; h2 → `{t("titleLead")} <span className="text-primary">{t("titleAccent")}</span>`; párrafo → `{t("description")}`; `alt`/`caption` de `FleetImage` → `{t("imageAlt")}`/`{t("imageCaption")}`; chip overlay `Cuidamos cada unidad como esta` → `{t("overlay")}`.

- [ ] **Step 2: about-section**

`const t = useTranslations("About")`. Reemplazos: kicker, title, párrafo intro → `{t("subtitle")}`; `FleetImage` alt/caption → `fleetAlt`/`fleetCaption`; chips overlay `Flota propia` → `{t("fleetChip")}` y `Terminal · Apodaca, N.L.` → `{t("fleetLocation")}`; `Nuestra Misión`/texto → `{t("missionTitle")}`/`{t("missionText")}`; `Nuestra Visión`/texto → `{t("visionTitle")}`/`{t("visionText")}`; `Nuestros Valores` → `{t("valuesTitle")}`; el array inline de valores → `{[0, 1, 2, 3, 4, 5].map((i) => ...{t(`values.${i}`)}...)}` con `key={i}`.

- [ ] **Step 3: coverage-section**

`const t = useTranslations("Coverage")` en `CoverageSection` y `const t = useTranslations("Coverage")` también en `CityPanel`. Cambios:

1. Los objetos `mxCities`/`usCities` cambian de claves display a claves estables (las listas de ciudades no se traducen):
```tsx
const mxCities = {
  norte: ["Monterrey", "Saltillo", "Chihuahua", "Nuevo Laredo"],
  bajio: ["Querétaro", "Guanajuato", "Aguascalientes", "San Luis Potosí"],
  pacifico: ["Baja California", "Sonora", "Sinaloa"],
  occidente: ["Guadalajara", "Michoacán", "Colima"],
}
const usCities = {
  texas: ["Laredo", "Houston", "Dallas", "San Antonio"],
  midwest: ["Chicago", "Detroit", "Indianapolis", "Kansas City"],
  southeast: ["Atlanta", "Miami", "Nashville", "Orlando"],
  west: ["Los Angeles", "Phoenix", "San Diego", "Seattle"],
}
```
2. `CityPanel` recibe una prop nueva `zonePrefix: "mxZones" | "usZones"` y renderiza el nombre de zona con `{t(`${zonePrefix}.${zone}`)}`; el contador → `{t("citiesCount", { count: Object.values(cities).flat().length })}`. Llamadas: `<CityPanel title={t("mxTitle")} cities={mxCities} zonePrefix="mxZones" accent="text-primary" />` y `<CityPanel title={t("usTitle")} cities={usCities} zonePrefix="usZones" accent="text-secondary" />`.
3. Strings sueltos: kicker → `{t("kicker")}`, title, subtitle, `Corredor principal` → `{t("console")}`, `MTY → LAREDO → HOU` → `{t("consoleTag")}`, `aria-label` del svg → `{t("mapAria")}`, texto svg `FRONTERA MX · USA` → `{t("borderLabel")}`, chip `1,180 km · ETA 17 h` → `<span className="text-yellow-accent-bright">●</span> {t("telemetry")}`, chip `Cruce B1 · doble placa` → `{t("crossingChip")}`. Las etiquetas de ciudades del SVG (`MONTERREY`, `HOUSTON`, …) son nombres propios y no se traducen.

- [ ] **Step 4: testimonials-section**

`const t = useTranslations("Testimonials")`. El array `testimonials` conserva solo `company`:
```tsx
const companies = ["Ternium", "Villacero", "Aceros BC"]
```
Map: `companies.map((company, i) => ...)` con `key={company}`, quote → `"{t(`items.${i}.quote`)}"`, `{company}`, role → `{t(`items.${i}.role`)}`. Kicker/title/subtitle → claves homónimas.

- [ ] **Step 5: Verificar build y commit**

Run: `pnpm build` — Expected: exitoso.
```bash
git add components/talleres-section.tsx components/about-section.tsx components/coverage-section.tsx components/testimonials-section.tsx
git commit -m "feat(i18n): talleres, about, coverage y testimonials consumen mensajes"
```

---

### Task 7: CTA (formularios) y Footer traducidos

**Files:**
- Modify: `components/cta-section.tsx`, `components/footer.tsx`

**Interfaces:**
- Consumes: namespaces `CTA`, `Footer`, `Header` (nav del footer reutiliza `Header.nav`) — Task 2.

- [ ] **Step 1: cta-section**

`const t = useTranslations("CTA")`. Reemplazos, formulario de cotización (prefijo `quote.`): kicker → `{t("quote.kicker")}`, h3 → `{t("quote.title")}`, párrafo → `{t("quote.description")}`, y cada Label/placeholder:
- `Nombre completo` → `{t("quote.name")}` / `placeholder={t("quote.namePh")}`
- `Correo electrónico` → `{t("quote.email")}` / `placeholder={t("quote.emailPh")}`
- `Teléfono` → `{t("quote.phone")}` / `placeholder={t("quote.phonePh")}`
- `Empresa` → `{t("quote.company")}` / `placeholder={t("quote.companyPh")}`
- `Origen` → `{t("quote.origin")}` / `placeholder={t("quote.originPh")}`
- `Destino` → `{t("quote.destination")}` / `placeholder={t("quote.destinationPh")}`
- `Detalles de la carga` → `{t("quote.details")}` / `placeholder={t("quote.detailsPh")}`
- botón `Enviar solicitud` → `{t("quote.submit")}`

Formulario de postulación (prefijo `apply.`): kicker, h3 `Únete a nuestro equipo` → `{t("apply.title")}`, párrafo → `{t("apply.description")}`, name/namePh, email/emailPh, phone/phonePh, `Puesto de interés` → `{t("apply.position")}`/`{t("apply.positionPh")}`, `Tu experiencia` → `{t("apply.experience")}`/`{t("apply.experiencePh")}`, `Adjuntar CV (opcional)` → `{t("apply.cv")}`, botón → `{t("apply.submit")}`.

Card de WhatsApp: h3 → `{t("whatsapp.title")}`, párrafo → `{t("whatsapp.description")}`, botón → `{t("whatsapp.button")}`.

- [ ] **Step 2: footer**

`const t = useTranslations("Footer")` y `const tNav = useTranslations("Header.nav")`. Reemplazos: tagline → `{t("tagline")}`; `Enlaces Rápidos` → `{t("quickLinksTitle")}`; los 5 links reutilizan `tNav`: `{tNav("inicio")}`, `{tNav("quienesSomos")}`, `{tNav("servicios")}`, `{tNav("cobertura")}`, `{tNav("clientes")}`; `Servicios` → `{t("servicesTitle")}` y la lista → `{[0, 1, 2, 3, 4].map((i) => <li key={i}>{t(`services.${i}`)}</li>)}`; `Contacto` → `{t("contactTitle")}`; dirección → `{t("addressLine1")}<br />{t("addressLine2")}`; copyright → `&copy; {new Date().getFullYear()} ServiExpress JC LLC. {t("rights")}`. Los `aria-label` de redes sociales (`Instagram`, `Facebook`, `LinkedIn`) son nombres propios — no cambian.

- [ ] **Step 3: Verificar que no queda copy hardcodeado**

Run:
```bash
grep -rnE "Cotización|Postúlate|Solicita|Nuestra|Quiénes|carga|México" components/*.tsx --include="*.tsx" | grep -v "components/ui/" | grep -vE "useTranslations|t\(|//|\* " || echo "LIMPIO"
```
Expected: `LIMPIO` (o solo coincidencias en comentarios/props no visibles; revisar cada hit restante y migrarlo si es texto visible).

- [ ] **Step 4: Verificar build y commit**

Run: `pnpm build` — Expected: exitoso.
```bash
git add components/cta-section.tsx components/footer.tsx
git commit -m "feat(i18n): formularios CTA y footer consumen mensajes"
```

---

### Task 8: SEO — metadata localizada, hreflang, sitemap, robots y JSON-LD

**Files:**
- Modify: `app/[locale]/layout.tsx`
- Create: `app/sitemap.ts`, `app/robots.ts`, `lib/site.ts`

**Interfaces:**
- Consumes: namespace `Metadata` (Task 2), `routing` (Task 1).
- Produces: `SITE_URL` y `localePath(locale)` en `lib/site.ts`, usados por layout y sitemap.

- [ ] **Step 1: Constantes de sitio**

`lib/site.ts`:
```ts
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://serviexpressjc.com.mx"

/** Ruta pública por locale: es vive en la raíz (localePrefix as-needed). */
export const localePath = (locale: string) => (locale === "es" ? "/" : `/${locale}`)
```

- [ ] **Step 2: generateMetadata completo con hreflang y Open Graph**

Reemplazar `generateMetadata` en `app/[locale]/layout.tsx` por:
```tsx
export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>
}): Promise<Metadata> {
  const { locale } = await params
  const t = await getTranslations({ locale, namespace: "Metadata" })
  const path = localePath(locale)

  return {
    metadataBase: new URL(SITE_URL),
    title: t("title"),
    description: t("description"),
    generator: "v0.app",
    alternates: {
      canonical: path,
      languages: {
        "es-MX": "/",
        "en-US": "/en",
        "x-default": "/",
      },
    },
    openGraph: {
      type: "website",
      siteName: "ServiExpress JC",
      url: path,
      title: t("title"),
      description: t("description"),
      locale: locale === "es" ? "es_MX" : "en_US",
      alternateLocale: locale === "es" ? "en_US" : "es_MX",
      images: [{ url: "/fleet/flota-patio.jpg", width: 1200, height: 675, alt: "ServiExpress JC" }],
    },
    twitter: {
      card: "summary_large_image",
      title: t("title"),
      description: t("description"),
      images: ["/fleet/flota-patio.jpg"],
    },
  }
}
```
con el import agregado: `import { SITE_URL, localePath } from "@/lib/site"`.

- [ ] **Step 3: JSON-LD localizado en el layout**

En `app/[locale]/layout.tsx`, dentro de `RootLayout` antes del `return`, construir el objeto (usa `getTranslations` ya importado):
```tsx
const tMeta = await getTranslations({ locale, namespace: "Metadata" })
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "ServiExpress JC",
  url: SITE_URL,
  logo: `${SITE_URL}/logo-white-bg.png`,
  description: tMeta("description"),
  email: "contacto@serviexpressjc.com.mx",
  telephone: "+13463669867",
  address: {
    "@type": "PostalAddress",
    streetAddress: "Carretera Mezquital Santa Rosa Km 05",
    addressLocality: "Apodaca",
    addressRegion: "Nuevo León",
    addressCountry: "MX",
  },
  areaServed: [
    { "@type": "Country", name: "Mexico" },
    { "@type": "Country", name: "United States" },
  ],
  sameAs: [
    "https://instagram.com/serviexpressjc1",
    "https://facebook.com/serviexpressjc",
    "https://linkedin.com/company/serviexpressjc",
  ],
}
```
y en el `<body>`, antes de `<NextIntlClientProvider>`:
```tsx
<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} />
```

- [ ] **Step 4: sitemap y robots**

`app/sitemap.ts`:
```ts
import type { MetadataRoute } from "next"
import { SITE_URL } from "@/lib/site"

export default function sitemap(): MetadataRoute.Sitemap {
  const languages = {
    "es-MX": SITE_URL,
    "en-US": `${SITE_URL}/en`,
  }
  return [
    { url: SITE_URL, lastModified: new Date(), changeFrequency: "monthly", priority: 1, alternates: { languages } },
    { url: `${SITE_URL}/en`, lastModified: new Date(), changeFrequency: "monthly", priority: 1, alternates: { languages } },
  ]
}
```

`app/robots.ts`:
```ts
import type { MetadataRoute } from "next"
import { SITE_URL } from "@/lib/site"

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: "*", allow: "/" },
    sitemap: `${SITE_URL}/sitemap.xml`,
  }
}
```

- [ ] **Step 5: Verificar build y commit**

Run: `pnpm build` — Expected: exitoso, con `/sitemap.xml` y `/robots.txt` en el output de rutas.
```bash
git add app/ lib/site.ts
git commit -m "feat(seo): metadata localizada, hreflang, sitemap, robots y JSON-LD"
```

---

### Task 9: Verificación integral en ambos idiomas

**Files:** ninguno nuevo (solo fixes que surjan).

- [ ] **Step 1: Build de producción y servidor**

```bash
pnpm build && (pnpm start &) && sleep 4
```
Expected: build exitoso, servidor en `http://localhost:3000`.

- [ ] **Step 2: Verificar HTML servido en ambas rutas**

```bash
curl -s http://localhost:3000/ | grep -o 'lang="es"' && \
curl -s http://localhost:3000/ | grep -o 'hreflang="en-US"' && \
curl -s http://localhost:3000/ | grep -c "Eficiencia sobre ruedas"
curl -s http://localhost:3000/en | grep -o 'lang="en"' && \
curl -s http://localhost:3000/en | grep -o 'hreflang="es-MX"' && \
curl -s http://localhost:3000/en | grep -c "Efficiency on wheels"
curl -s http://localhost:3000/sitemap.xml | grep -c "xhtml:link"
curl -s http://localhost:3000/robots.txt | grep -o "sitemap"
curl -s http://localhost:3000/ | grep -o 'application/ld+json'
```
Expected: cada grep devuelve al menos 1 coincidencia. El contenido EN debe estar en el HTML estático (no solo tras hidratación).

- [ ] **Step 3: Verificación funcional en preview**

Con las herramientas de preview del entorno (o navegador): en `/` y `/en` comprobar: (1) el switcher ES|EN alterna idioma manteniendo la posición; (2) navegación por anclas funciona en ambos; (3) el menú móvil muestra el switcher; (4) formularios muestran labels/placeholders del idioma correcto; (5) no hay desbordes de layout con los textos EN (revisar hero, stats, botones del header y cards de servicios en viewport móvil 375px).

- [ ] **Step 4: Matar servidor y commit final si hubo fixes**

```bash
kill %1 2>/dev/null || pkill -f "next start" || true
git status --short
```
Si hubo correcciones: `git add -A && git commit -m "fix(i18n): ajustes de verificación integral ES/EN"`.
