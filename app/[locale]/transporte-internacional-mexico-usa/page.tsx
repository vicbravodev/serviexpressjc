import { createContentRoute } from "@/lib/content-route"

const { generateMetadata, Page } = createContentRoute("/transporte-internacional-mexico-usa")
export { generateMetadata }
export default Page
